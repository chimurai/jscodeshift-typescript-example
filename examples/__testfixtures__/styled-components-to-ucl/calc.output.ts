import { Box } from '@rbilabs/universal-components';

export const PruneCalc = Box.withConfig({
  margin: '$40',

  // TODO: RN - unsupported CSS
  // padding: 'calc(10rem + 12px)',

  // TODO: RN - unsupported CSS
  // maxHeight: 'calc(100vh + 1px)',
});